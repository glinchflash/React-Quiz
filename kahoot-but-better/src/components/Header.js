import React from 'react';
import './css/Header.css'

function Header() {
    const title = "Quiz it up";
    return (
        <header>
            <h1>{title}</h1>
        </header>
    );
}

export default Header;